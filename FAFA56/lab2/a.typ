#import "@preview/unify:0.6.0": qty

1. #[
  $ Delta y=(lambda L)/d <=> lambda = (d Delta y)/L = (qty("0,25e-3", "m") dot qty("2,5e-3", "m"))/qty("1,00", "m") = qty("625", "nm"). $ Mycket rimligt för en röd laser.
]
2. #[
  - $ d=1/"linjer per millimeter" $
  - #[
    $ Delta y=qty("20,8e-2", "m") $
    $ lambda=(d Delta y)/(L)=((qty("80", "mm^-1"))^(-1) dot qty("5,2e-2", "m"))/qty("1,00", "m")=qty("650", "nm") $
  ]
]
3. $ Delta y=(lambda L)/d <=> d=(lambda L)/(Delta y)=(qty("6,50e-7", "m") dot qty("1,00", "m"))/qty("2,5e-3", "m")=qty("0,26", "mm") $

