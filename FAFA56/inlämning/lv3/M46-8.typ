#import "../template.typ": m, e
#import "@preview/unify:0.6.0": qty, num

#show: doc => m(ch: 46, n: "8", doc)

Vi har osäkerheten för elektronens position $sigma x=2 Delta x$ och för dess rörelsemängd $sigma p_x=2 Delta p_x$.

Heisenbergs osäkerhetsprincip säger att

$ sigma x space sigma p_x >= h / (2 pi) <=> 2Delta x = sigma x>=h/(2 pi sigma p_x)=h/(4 pi Delta p_x) <=> Delta x>=h/(8 pi Delta p_x). $

Svar: *(C)*.
