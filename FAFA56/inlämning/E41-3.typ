#import "./template.typ": e
#import "@preview/unify:0.6.0": qty

#show: doc => e(ch: 41, n: "3", doc)

$L >> d$ så avståndet mellan intilliggande maxima kommer vara någorlunda konstant nära centralmaximum.

$ Delta y = (lambda L)/d = (qty("5,12e-7", "m") dot qty("5,4", "m"))/qty("1,2e-3", "m")=qty("2,3e-3", "m") $
